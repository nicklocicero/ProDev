<?xml version="1.0" encoding="utf-8"?>
<ScrollView
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="#00F">
<android.support.constraint.ConstraintLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  tools:context="edu.cnm.deepdive.nicklocicero.prodev.MainActivity">

  <TextView
    android:id="@+id/title"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="@string/pro_dev"
    android:textSize="24sp"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    android:fontFamily="sans-serif"
    android:textColor="#FFF"/>

  <TextView
    android:id="@+id/golden_circles_title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:layout_constraintTop_toBottomOf="@id/title"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    android:textSize="18sp"
    android:fontFamily="sans-serif"
    android:text="@string/golden_circles_title"
    android:textColor="#FFF"
    />
  <TextView
    android:id="@+id/golden_circles_description"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    app:layout_constraintTop_toBottomOf="@id/golden_circles_title"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    android:textSize="12sp"
    android:fontFamily="sans-serif"
    android:text="@string/golden_circles_description"
    android:textColor="#FFF"
    />

  <TextView
    android:id="@+id/qualitative_quantitative_title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:layout_constraintTop_toBottomOf="@id/golden_circles_description"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    android:textSize="18sp"
    android:fontFamily="sans-serif"
    android:text="@string/qualitative_vs_quantitative_title"
    android:textColor="#FFF"
    />
  <TextView
    android:id="@+id/qualitative_quantitative_descpription"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:layout_constraintTop_toBottomOf="@id/qualitative_quantitative_title"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    android:textSize="12sp"
    android:padding="8dp"
    android:fontFamily="sans-serif"
    android:text="@string/qualitative_quantitative_text"
    android:textColor="#FFF"
    />

</android.support.constraint.ConstraintLayout>
</ScrollView>