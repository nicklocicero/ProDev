<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  tools:context="edu.cnm.deepdive.nicklocicero.prodev.MainActivity"
  android:background="#00F">

  <TextView
    android:id="@+id/title"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="@string/pro_dev"
    android:textSize="24sp"
    android:paddingTop="48dp"
    app:layout_constraintBottom_toTopOf="@+id/text_scroll"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    android:textColor="#FFF"/>

  <ScrollView
    android:id="@+id/text_scroll"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="18dp"
    app:layout_constraintTop_toBottomOf="@id/title"
    app:layout_constraintBottom_toBottomOf="parent">
  <TextView
    android:id="@+id/prodev_learn"
    android:layout_width="wrap_content"
    android:layout_height="0dp"
    android:paddingBottom="32dp"
    android:text="@string/prodev_learn"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="@id/text_scroll"
    android:textColor="#FFF"
    />
  </ScrollView>

</android.support.constraint.ConstraintLayout>
